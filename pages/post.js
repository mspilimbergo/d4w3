import React from 'react'

export default function Post() {
  return (
    <div>
        {/* Form / Vertically split / trust section on the right */}
            {/* Trust section on the right -> populated with static data */}
        {/* Submit buttom */}
    </div>
  )
}

// Data Todos
// Create ID for post 
// Aggregate all form data and send to DB
